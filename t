the setup
  lerna init -i
  create two packages
  lerna bootstrap
  show linkage

lets add some code
  requires
  build tools
  watcher
  linting
  testing

the developer experience
  storybook
  styleguide
  livedemo?

adding new projects
  add a new project
  run bootstrap
  add a story
  add a test

let's make a change
  change button to allow theming similar to bootstrap
  watch change filter through system (closing components, then servers)